# Secrets — never commit these
.env
session_cookies.json
data.json
users.json
debug_page.html
debug_*.html

# Claude Code per-project settings (can record secrets in command allowlist)
.claude/

# Browser profile (contains auth cookies)
browser_session/

# Logs
logs/

# Runtime flags & test caches
.session_expired_notified
.pytest_cache/

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# Node (worker)
node_modules/
.wrangler/

# Local-only persistent state — Playwright user data dir
inbar_profile/
