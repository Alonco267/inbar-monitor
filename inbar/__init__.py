"""Shared library for the Inbar Grade Monitor.

Modules:
    textutils   — pure text/date helpers (no I/O)
    diff        — change-detection engine (pure functions)
    formatting  — Telegram message builders (pure functions)
    scrape      — Playwright grade/GPA extraction
    login       — Inbar/ADFS/portal/OTP login flows
    relay       — Cloudflare Worker relay HTTP client
"""
